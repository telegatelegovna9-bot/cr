'use client';

export function PatternsView() {
  return (
    <div className="h-full p-3 min-h-0">
      <div className="glass-card flex h-full min-h-0 items-center justify-center p-8">
        <div className="max-w-xl text-center">
          <div className="text-base font-semibold text-text-primary">Patterns tab is intentionally empty</div>
          <div className="mt-2 text-sm leading-relaxed text-text-muted">
            Previous pattern scanning UI and backend integration were removed to leave a clean surface for a new product.
          </div>
        </div>
      </div>
    </div>
  );
}
